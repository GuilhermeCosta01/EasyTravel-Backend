spring.application.name=ET-EasyTravel


# Configuracoes do SQL Server
#spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=DB_ET;encrypt=false;trustServerCertificate=true
#spring.datasource.username=sa
#spring.datasource.password=Guinne001@


# Configuracoes do SQL Server
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.url=jdbc:sqlserver://easy-travel.database.windows.net:1433;database=banco-et;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=EasyTravel
spring.datasource.password=SenhaSegura123

# Configuracoes do JPA/Hibernate para SQL Server
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# Configuracao de encoding
spring.datasource.hikari.connection-test-query=SELECT 1

# Profile ativo
spring.profiles.active=prod

# Configuracoes do Swagger/OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

# Configuracoes adicionais para SQL Server
spring.jpa.properties.hibernate.connection.characterEncoding=utf-8
spring.jpa.properties.hibernate.connection.useUnicode=true

# Security
spring.security.user.name=admin
spring.security.user.roles=USER

# JWT
api.security.token.secret=${JWT_SECRET:senhamuitosegura01}

# Configuracoes de email SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=eliomarvinicius7@gmail.com
spring.mail.password=hpya xglu iuqw zoqj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Configuracoes HTTPS

server.port=${PORT:8080}
server.use-forward-headers=true
server.forward-headers-strategy=framework
server.tomcat.remote-ip-header=x-forwarded-for
server.tomcat.protocol-header=x-forwarded-proto

# SSL desabilitado para Azure (Azure gerencia HTTPS)
server.ssl.enabled=false

# Configuracoes de CORS para Azure App Service
management.endpoints.web.cors.allowed-origins=https://back-et.azurewebsites.net,http://localhost:4200,http://localhost:3000
management.endpoints.web.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
management.endpoints.web.cors.allowed-headers=*
management.endpoints.web.cors.allow-credentials=true

# Configuracoes de CORS para Azure App Service
spring.web.cors.allowed-origins=https://back-et.azurewebsites.net,http://localhost:4200,http://localhost:3000,http://localhost:8080,https://localhost:8080
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600
